[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Configure function timeout using the correct syntax
[[functions]]
  path = "/api/projects/*/analyze"
  maxDuration = 60